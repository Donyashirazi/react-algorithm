// import React from 'react';

// const IndividualBox = ({ items, lastChild }) => {
// 	console.log('lastChild,', lastChild);

// 	const createNumbers = () => {
// 		const Array = [];

// 		for (let i = lastChild; i <= items.length; i++) {
// 			Array.push(i);
// 		}

// 		return Array;
// 	};

// 	return items.map((lastChild) => (
// 		<div className="grid">{createNumbers()}</div>
// 	));
// };

// export default IndividualBox;
